# AGENTS.md

## 项目技术栈

本项目是基于 **Tauri + React + TypeScript** 构建的桌面端应用。

核心技术栈：

- **Tauri**：桌面框架、系统能力及原生交互
- **React**：前端 UI
- **TypeScript**：前端主要开发语言
- **Ant Design**：UI 组件库
- **Vite**：前端开发与构建
- **Rust**：Tauri 后端及系统能力实现
- **pnpm**：包管理工具

---

## 项目定位

项目负责统一管理多个独立功能模块，并通过桌面端 UI 提供应用入口、配置、控制、状态展示及任务管理能力。

架构原则：

- React / TypeScript 负责 UI、交互和前端状态管理。
- Tauri / Rust 负责系统能力、原生能力及前后端通信。
- 各业务模块保持独立，避免不必要的跨模块依赖。
- 公共能力仅在多个模块实际复用时提升到公共层。
- 优先保证可维护性、职责边界和结构一致性。
- 不得为了实现方便破坏既有架构边界。

---

## 项目目录结构

```text
ChrysosHeirs/
├── .agents/
│   └── skills/
│       └── antd/
│           └── SKILL.md
├── applications/
├── docs/
├── log/
├── public/
├── runtime/
├── scripts/
├── src/
│   ├── app/
│   ├── assets/
│   ├── modules/
│   │   ├── apps/
│   │   │   ├── Aglaea/
│   │   │   ├── Cipher/
│   │   │   └── Tribios/
│   │   └── Overview/
│   ├── router/
│   ├── shared/
│   └── styles/
└── src-tauri/
    ├── capabilities/
    ├── icons/
    ├── src/
    │   ├── commands/
    │   ├── lib.rs
    │   └── main.rs
    ├── build.rs
    ├── Cargo.toml
    └── tauri.conf.json
```

顶层目录职责：

- `.agents/`：Agent 使用的本地 Skill。
- `applications/`：平台实际管理、加载或运行的独立应用资源，不存放 React 页面。
- `docs/`：项目文档。
- `log/`：Agent、代码检查、审查及开发分析日志。
- `runtime/`：项目运行时及相关依赖，与源码分离。
- `scripts/`：构建、自动化、开发及维护脚本。
- `src/`：React 前端代码。
- `src-tauri/`：Tauri / Rust 原生端代码。

目录结构描述当前项目状态。不得为了所谓“标准架构”提前创建没有实际用途的目录。

---

## Agent 工作原则

新增、修改、移动或重构代码前必须：

1. 先查看相关模块已有实现。
2. 优先沿用项目当前正在使用的实现方式。
3. 不重复实现已有能力。
4. 不为未来可能存在的需求提前抽象。
5. 不为了目录完整性创建无实际用途的目录。
6. 不为了减少工作量破坏模块职责。
7. 不随意扩大当前任务修改范围。
8. 重构默认保持现有行为，除非任务明确要求改变。
9. 新增依赖前先确认现有依赖是否能够满足需求。
10. 历史代码与本文件硬性规则冲突时，新代码必须以本文件为准。
11. 优先参考较新的、仍在使用且结构清晰的实现，不机械复制历史遗留代码。

---

## Ant Design 强制规则

涉及 Ant Design、页面 UI 或页面布局开发时，**编写代码前必须读取**：

```text
.agents/skills/antd/SKILL.md
```

必须按照 Skill 中的要求实现。

### Ant Design 布局优先

页面基础布局和常见 UI 必须优先评估 Ant Design 已有能力。

优先使用包括但不限于：

- `Row`
- `Col`
- `Flex`
- `Space`
- `Card`
- `Table`
- `Layout`
- `Form`
- `Descriptions`
- `List`
- `Grid`

常见场景包括：

- 横向 / 纵向排列
- 间距
- 栅格布局
- 卡片容器
- 表格
- 表单
- 信息展示
- 页面骨架

硬性要求：

- `Row / Col` 可以完成的栅格布局，不无必要重写 Grid。
- `Flex / Space` 可以完成的排列和间距，不无必要增加布局 SCSS。
- `Card` 已满足容器需求时，不自行重复实现通用 Card。
- 标准结构化数据优先使用 `Table`，禁止无必要使用大量 `div` 模拟表格。
- Ant Design 已有成熟基础组件时，不重复造轮子。
- 不得仅因为写 `div` 更快而绕过 Ant Design。

仅在 Ant Design 无法合理满足特殊视觉、定位、动画、叠层、复杂响应式或高度定制布局时，使用自定义 CSS / SCSS。

原则：

> Ant Design 能清晰完成的基础 UI 不重复实现；Ant Design 不适合的复杂布局再使用自定义样式。

---

## React 页面强制规则

### 开发前检查

新增页面或大幅修改页面前，必须先查看：

- 当前业务模块已有页面。
- 当前业务模块已有组件。
- 相邻模块近期页面实现。
- 项目当前正在使用的 UI 和代码组织方式。

历史实现与近期实现明显不同时，优先参考较新的、仍在使用且结构更清晰的实现。

### 页面必须合理组件化

**禁止将复杂页面长期堆积在单个大型 `.tsx` 文件中。**

涉及页面、Layout 或大型 UI 组件时，首次编辑代码前必须先完成职责检查，并明确：

1. 页面或 Layout 自身负责什么。
2. 存在哪些可以独立命名的视觉区域。
3. 哪些区域应拆为页面或 Layout 私有组件。
4. 每个组件的状态、交互和样式归属。

页面级组件主要负责：

- 页面整体结构
- 页面级数据获取
- 页面级状态协调
- 页面级事件编排
- 页面级业务流程协调
- 子组件组合

具体 UI 和局部交互应按职责拆分为独立组件。

出现以下任一情况时必须拆分，不得仅“考虑拆分”：

- 区域具有明确且独立的 UI 职责。
- 区域可以被明确命名，例如 Header、Sider、Footer、Toolbar、Navigation、Hero、Content。
- 区域拥有自己的 Props、局部状态、事件或导航逻辑。
- 区域拥有独立样式文件或大量专属样式。
- JSX 层级明显复杂。
- 页面存在多个独立视觉模块。
- UI 细节已经影响页面主流程可读性。
- 相同 UI 已经实际出现多次。
- 参考实现已经拆分该区域，且当前项目不存在不适合拆分的明确原因。

组件拆分不以代码行数作为唯一标准。

用户指定修改某个 `.tsx` 文件，只表示该文件是任务入口，不表示所有实现都必须堆积在该文件中。

### Layout 文件职责

Layout 组件只允许负责：

- Ant Design `Layout` 的整体组合。
- 页面级路由状态协调。
- `Outlet` 的承载。
- 面包屑等与整体路由直接相关的结构。
- Header、Sider、Footer 等子组件的组合。

当 Header、Sider、Footer、Navigation 或 Toolbar 包含实际内容、专属样式或交互时，必须拆成 Layout 私有组件。

禁止在单个 Layout 文件中同时实现：

- 完整顶栏 UI。
- 完整侧栏 UI。
- 导航配置与选中状态。
- 大量区域专属样式。
- 多个独立视觉模块。

Layout 拆出的组件必须保留在当前 Layout 的私有目录中。例如：

```text
layout/
├── components/
│   ├── LayoutHeader/
│   │   ├── LayoutHeader.tsx
│   │   └── LayoutHeader.module.scss
│   └── LayoutSider/
│       ├── LayoutSider.tsx
│       └── LayoutSider.module.scss
├── Layout.tsx
└── Layout.module.scss
```

只有产生真实职责时才创建对应组件；不得为了目录形式创建空组件。

子组件的专属样式必须放在子组件自己的样式文件中。Layout 的样式文件只保留整体布局、内容区域和组件之间的关系。

### 禁止过度组件化

不得为了形式上的组件化创建：

- 没有独立职责的碎片组件。
- 仅包裹简单元素的无意义组件。
- 只为了减少页面代码行数而存在的组件。

核心原则：

> 页面负责组合与编排，子组件负责具体 UI 和局部交互。

### 组件作用域

组件必须保留在能够满足实际使用范围的最小作用域：

- 仅单页面使用：保持页面私有。
- 同一业务模块多个页面实际复用：提升到模块共享层。
- 多个业务模块实际复用：再考虑提升到项目公共层。

禁止以“以后可能复用”为理由提前公共化。

### 编辑前与交付前检查

首次编辑页面或 Layout 前，Agent 必须简要说明准备如何划分职责。

交付前必须检查：

1. 页面或 Layout 是否只负责组合与编排。
2. 是否存在仍应拆分的独立视觉区域。
3. 是否为了满足用户指定文件而把实现集中到单文件。
4. 是否优先使用了 Ant Design 已有布局能力。
5. 每个组件和样式是否保持在最小合理作用域。
6. 是否通过 `antd lint` 和 `pnpm build`。

任一项不满足时，不得直接交付，必须先修正。

---

## Tauri / Rust 强制规则

Rust 侧必须明确分离：

- IPC 暴露层
- 实际业务逻辑
- 系统能力

### `src-tauri/src/commands/`

**`commands/` 只能作为 Tauri IPC Adapter。**

Command 仅允许：

1. 接收前端参数。
2. 进行必要的轻量参数转换。
3. 调用实际 Rust 业务实现。
4. 转换返回结果。
5. 进行必要的错误映射。

**Command 中禁止实现任何实际业务逻辑。**

包括但不限于：

- 核心业务判断
- 文件扫描和核心文件处理
- Runtime 管理
- 进程管理
- 数据库业务流程
- 数据持久化业务编排
- 状态管理核心逻辑
- 核心计算
- 长流程任务编排
- 可被其他 Rust 模块复用的业务逻辑

判断标准：

> Rust 代码脱离 Tauri Command 后仍具有独立业务意义、领域意义或复用价值，就必须从 `commands/` 中拆出。

不得以“代码少”“暂时只有这里使用”“实现方便”“以后再拆”等理由违反该规则。

调用方向必须保持：

```text
React
  ↓
Tauri IPC
  ↓
commands
  ↓
Rust 业务模块
  ↓
System / Runtime / Storage / Process
```

业务层禁止反向依赖 `commands/`。

实际 Rust 业务代码根据真实职责按需拆分，例如：

```text
src-tauri/src/
├── commands/
├── services/
├── runtime/
├── process/
├── storage/
├── models/
├── lib.rs
└── main.rs
```

该结构仅为示意，不要求提前创建。

只有实际产生对应职责时才创建模块，禁止为了所谓“标准 Rust 架构”创建空目录。

---

## 最高优先级规则

以下规则不得绕过：

1. `src-tauri/src/commands/` 只能作为 IPC Adapter，禁止业务逻辑。
2. Rust 业务层禁止反向依赖 `commands/`。
3. React 复杂页面必须进行合理组件拆分。
4. 页面负责组合与编排，子组件负责具体 UI 和局部交互。
5. 禁止过度组件化和提前公共化。
6. 页面开发前必须参考项目当前较新的实现风格。
7. 涉及 Ant Design 或页面 UI 开发时，必须先读取 `.agents/skills/antd/SKILL.md`。
8. 页面基础布局和常见 UI 必须优先使用 Ant Design 已有组件能力。
9. 不为未来需求提前抽象。
10. 不创建没有实际用途的目录或模块。
11. 不重复实现已有能力。
12. 不得为了实现方便绕过已经明确的架构边界。

## 代码质量与性能强制规则

项目目标不是“代码能运行即可”。

Agent 编写或重构代码时，必须同时保证：

- 正确性
- 可读性
- 可维护性
- 高内聚
- 低耦合
- 合理的时间复杂度
- 合理的空间复杂度
- 较低的 IO、内存分配和无效计算开销

### 核心原则

代码设计应优先保持 **高内聚、低耦合**：

- 一个模块、组件、函数只承担清晰且相关的职责。
- 业务逻辑应尽量聚合在所属领域内部。
- 避免模块之间产生不必要的直接依赖。
- 避免为了复用少量代码建立不合理的跨模块依赖。
- 公共抽象必须建立在真实复用需求之上。
- 不允许通过全局状态、跨层调用或隐式依赖绕过正常架构边界。
- 模块之间优先通过清晰、稳定、最小化的接口通信。
- 修改一个功能时，应尽量避免要求多个无关模块同时发生变化。

### 性能优先原则

**在不降低代码正确性、可读性、可维护性，不破坏高内聚低耦合的前提下，应优先选择性能更好的实现。**

存在多个同样清晰、可靠、可维护的方案时，应优先选择：

- 时间复杂度更低的方案。
- 空间复杂度更合理的方案。
- 更少重复计算的方案。
- 更少 IO 的方案。
- 更少内存分配和数据复制的方案。
- 更少无效 React Render 的方案。
- 更少锁竞争和阻塞的方案。
- 更适合当前访问模式的数据结构。

例如：

- 能使用 O(log n) 的二分查找时，不无理由使用 O(n) 全量扫描。
- 能使用 `Set / Map` 完成高频查找时，不反复使用 `Array.find / includes`。
- 能一次遍历完成时，不无理由进行多次完整遍历。
- 能消除嵌套循环时，不保留明显不必要的 O(n²) 实现。
- 能批量 IO 时，不逐条执行大量小型 IO。
- 能避免重复计算时，不在热点路径重复执行相同逻辑。

### 禁止以性能为理由破坏代码质量

性能优化不得导致：

- 职责混乱。
- 模块强耦合。
- 大量隐式状态。
- 难以理解的控制流。
- 不必要的复杂算法。
- 难以维护的微优化。
- 明显增加 Bug 风险。

对于数据量很小、低频执行、非热点路径且性能差异可以忽略的代码，不要求为了理论复杂度引入明显更复杂的实现。

核心判断：

> 当性能优化具有实际价值且不会降低可读性、可维护性或架构质量时，选择性能更优的方案；否则优先保持代码清晰和职责正确。

### Agent 性能检查

涉及大量数据、循环、搜索、排序、数据库、文件、IPC、进程或高频 UI 更新时，Agent 必须主动检查：

1. 是否存在明显更低的时间复杂度。
2. 是否存在不必要的嵌套遍历。
3. 是否存在重复计算。
4. 数据结构是否适合实际访问模式。
5. 是否存在可减少的 IO。
6. 是否存在可避免的数据复制或内存分配。
7. 是否存在无意义 React Render。
8. 性能优化是否会破坏高内聚低耦合。
9. 是否存在同样清晰但性能更好的实现。

不得因为当前实现“已经能跑”而保留明显低效的实现。

### 总体优先级

发生设计取舍时，按照以下优先级判断：

> **正确性 → 可读性 / 可维护性 → 高内聚低耦合 → 性能**

在前面的要求均能满足时：

> **性能更优的方案优先。**
